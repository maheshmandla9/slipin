node_modules
dist
.vercel
.env
.env.*
!.env.example
*.log
.DS_Store

# Local Claude Code chat archives (mm-chat-archive) — kept on disk for records,
# not committed: raw transcripts can contain pasted secrets and are large.
chat/

.vercel
